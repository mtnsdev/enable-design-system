name: enable_design_system
description: A comprehensive design system for the Enable platform with standardized components, design tokens, and consistent UI/UX.
version: 1.0.0
homepage: https://github.com/mtnsdev/enable-design-system

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  widgetbook: ^3.8.0
  widgetbook_annotation: ^3.1.0
  build_runner: ^2.4.0
  widgetbook_generator: ^3.7.0

flutter:
  uses-material-design: true


